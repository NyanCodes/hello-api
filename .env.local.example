# Copy to .env.local only if you do not already have one.
MONGODB_URI=mongodb://localhost:27017
DB_NAME=week10
ADMIN_USER=
ADMIN_PASS=
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
FRONTEND_ORIGIN=http://localhost:5173
# Next.js sets NODE_ENV automatically for dev/build/start.
